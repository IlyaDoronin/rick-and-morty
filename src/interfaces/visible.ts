export interface IVisible {
    id: { name: string; state: boolean };
    name: { name: string; state: boolean };
    air_date: { name: string; state: boolean };
    episode: { name: string; state: boolean };
    characters: { name: string; state: boolean };
}
